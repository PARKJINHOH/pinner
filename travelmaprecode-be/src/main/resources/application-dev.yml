server:
  port: 8081
spring:
  datasource:
    url: jdbc:mariadb://144.24.67.233:3306/pinner_dev
    username: ${DB_ID}
    password: ${DB_PW}
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop # TODO update 변경

  security:
    oauth2:
      client:
        registration:
          github:
            clientId: test # TODO 삭제
            clientSecret: test # TODO 삭제
            scope:
              - user:email

path:
  image: images
  url: https://pinner.dev
  